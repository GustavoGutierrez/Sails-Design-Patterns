module.exports.connections = {
 mongoDb: {
    adapter: 'sails-mongo',
    host: 'r2.ecs.network', // defaults to `localhost` if omitted
    port: 27017, // defaults to 27017 if omitted
    user: 'NodrizaDev', // or omit if not relevant
    password: 'shox009009', // or omit if not relevant
    database: 'NodrizaDev' // or omit if not relevant
  }
};
